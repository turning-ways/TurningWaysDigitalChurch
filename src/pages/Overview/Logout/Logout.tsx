import OverviewContainer from "../OverviewContainer";

const Logout = () => {
  return (
    <OverviewContainer active="Logout">
      <div className="flex w-full justify-center items-center">
        <p>Logout</p>
      </div>
    </OverviewContainer>
  );
};

export default Logout;
