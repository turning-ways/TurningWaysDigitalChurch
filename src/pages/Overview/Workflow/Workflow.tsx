import OverviewContainer from "../OverviewContainer";

const Workflow = () => {
  return (
    <OverviewContainer active="Workflow">
      <div className="flex w-full justify-center items-center">
        <p>Workflow</p>
      </div>
    </OverviewContainer>
  );
};

export default Workflow;
